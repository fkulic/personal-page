$(function(){
	let typedStrings = [""]

	$("#quoteStrings").typed({
		strings: [" android applications.", " web applications.", " web pages.", " arduino projects."],
		startDelay: 100,
		typeSpeed: 50,
		backDelay: 1400,
		loop: true
	});
});
